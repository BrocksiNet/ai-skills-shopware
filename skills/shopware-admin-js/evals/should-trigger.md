# shopware-admin-js — should trigger

- Add an Administration Vue module that opens mt-modal.
- Write a Jest spec next to this Admin TypeScript component.
- This Admin UI reads a new DAL association — update ACL privileges.
- Convert this new Administration file to TypeScript.
- Should I replace mt-modal with a native dialog in the Admin?
