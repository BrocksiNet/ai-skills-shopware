# Task: add a RELEASE_INFO entry under the existing heading

`RELEASE_INFO-6.7.md` already has a `## Features` section under `# 6.7.4.0`.

- Add a developer-facing note that `CartProcessor` is now extensible.
- Put that note under the existing `## Features` heading.
- Do not add a second `## Features` heading or a second version title.
- Keep the existing `ProductRoute` entry.
