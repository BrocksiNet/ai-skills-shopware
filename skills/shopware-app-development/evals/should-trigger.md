# shopware-app-development — should trigger

- Build a Shopware app with a manifest.xml that adds a custom field.
- Write an app script that runs on the product-page-loaded hook.
- Register a webhook so my app reacts when an order is placed.
- What permissions does my app need to read orders via the Admin API?
- Should this integration be a Shopware app or a plugin?
- Add an Admin UI module to my app with the Meteor Admin SDK.
- My app under custom/apps fails app:validate — what's wrong with the manifest?
