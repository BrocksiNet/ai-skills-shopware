# Task: Store-API OpenAPI

Add OpenAPI documentation to `fixture/ExampleRoute.php` for the Store-API route.
Use `OpenApi\Attributes` (or equivalent `OpenApi\` namespace) so the route is
documented for headless clients.
