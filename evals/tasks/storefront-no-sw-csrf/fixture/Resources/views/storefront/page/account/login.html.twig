<form action="{{ path('frontend.account.login') }}" method="post">
    {{ sw_csrf('frontend.account.login') }}
    <input type="email" name="email">
    <button type="submit">{{ 'account.loginSubmit'|trans }}</button>
</form>
