# php-foundation — should trigger

Prompts where this skill is expected to activate. One prompt per bullet.

- Refactor this Shopware service to use strict types and proper return types.
- Should this list of order states be an enum or class constants?
- This method returns an associative array with fixed keys — turn it into a DTO.
- Make this value object immutable with constructor property promotion.
- PHPStan complains about a mixed type here; how should I type this collection?
- Convert this `array<int, ProductEntity>` to a `list<>` and add proper generics.
