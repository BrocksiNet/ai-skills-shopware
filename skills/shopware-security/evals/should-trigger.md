# shopware-security — should trigger

- Can I hardcode the sales channel access key in this config class?
- Review this Store-API route for ACL and login requirements.
- We logged the sw-access-key in the request — is that safe?
- App manifest permissions — are we over-permissioned?
- Verify webhook signature on this app endpoint.
