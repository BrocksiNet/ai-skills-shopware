# Task: colocate the Admin Jest spec

`product-card.ts` is a new Administration TypeScript component without a
test.

- Add `product-card.spec.ts` in the same directory.
- Keep the spec under 500 lines. Split into `product-card.spec/` only if
  it would exceed that.
- Do not put the spec under `tests/`.
