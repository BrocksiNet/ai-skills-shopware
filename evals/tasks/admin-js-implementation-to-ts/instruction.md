# Task: convert the Admin implementation to TypeScript

`product-card.js` is a new Administration implementation module. New Admin
code is TypeScript.

- Convert `product-card.js` to `product-card.ts`. Keep the component export.
- Leave `Resources/app/administration/src/main.js` as JavaScript. That
  entry file is the only `.js` exception Shopware discovers.
- Do not add `main.ts`.
