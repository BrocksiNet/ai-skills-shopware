# Local eval/runner artifacts
evals/.runs/
evals/**/.out/
*.log

# Node (skills.sh runner helpers, markdown lint)
node_modules/
.npm/

# OS
.DS_Store

# Local environment / secrets (never commit API keys for the eval harness)
.env
.env.*
