# shopware-architecture — should not trigger

- Write a unit test for this validator (shopware-testing).
- Add strict_types and convert this constant class to an enum (php-foundation).
- Fill the shopware/shopware PR template (shopware-pr-description).
- Is it safe to commit this sales channel access key to git? (shopware-security)
- Build a Shopware plugin with a migration (shopware-plugin-development).
- Run phpunit inside podman (shopware-podman-dev).
