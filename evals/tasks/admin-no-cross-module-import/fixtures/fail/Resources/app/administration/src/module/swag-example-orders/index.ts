// Modules stay independent — no direct import from another module.
import OrderLine from '../swag-example-products/order-line';

export default {
    name: 'swag-example-orders',
    components: { OrderLine },
};
